<Window x:Class="BlobTrap.App.Views.QualityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolher qualidade" Height="620" Width="620"
        Background="{StaticResource Surface}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" ShowInTaskbar="False">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="SubtitleText" Style="{StaticResource Muted}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tracks -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceRaised}" CornerRadius="8"
                    BorderBrush="{StaticResource Border}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="QUALIDADE" FontSize="10" FontWeight="Bold"
                               Foreground="{StaticResource TextMuted}" Margin="14,12,0,6"/>

                    <ListBox Grid.Row="1" x:Name="VideoList" Background="Transparent" BorderThickness="0"
                             Margin="6,0,6,8" HorizontalContentAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="4,2"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Root" CornerRadius="6" Padding="10,8"
                                                    Background="Transparent" BorderThickness="1" BorderBrush="Transparent">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Root" Property="Background" Value="{StaticResource SurfaceHover}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Root" Property="Background" Value="{StaticResource SurfaceHover}"/>
                                                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="12.5"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SizeLabel}" Style="{StaticResource Muted}"
                                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Audio track -->
            <Grid Grid.Row="1" Margin="0,12,0,0" x:Name="AudioRow">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Faixa de audio" VerticalAlignment="Center"
                           Margin="0,0,12,0" FontSize="12"/>
                <ComboBox Grid.Column="1" x:Name="AudioCombo" DisplayMemberPath="Label"/>
            </Grid>

            <!-- Subtitles -->
            <StackPanel Grid.Row="2" x:Name="SubtitleRow" Margin="0,12,0,0">
                <TextBlock Text="LEGENDAS" FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                <ItemsControl x:Name="SubtitleList" MaxHeight="90">
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>

        <!-- Output -->
        <StackPanel Grid.Row="2" Margin="0,14,0,0">
            <CheckBox x:Name="AudioOnlyCheck" Content="Baixar somente o audio" Checked="OnAudioOnlyChanged"
                      Unchecked="OnAudioOnlyChanged"/>

            <TextBlock Text="SALVAR COMO" FontSize="10" FontWeight="Bold"
                       Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" x:Name="OutputBox"/>
                <Button Grid.Column="1" Content="..." Width="40" Margin="8,0,0,0" Click="OnBrowseClick"/>
            </Grid>
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Row="3" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="WarningText" Style="{StaticResource Muted}"
                       VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,12,0"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancelar" Width="100" Margin="0,0,8,0" Click="OnCancelClick"/>
                <Button Content="Baixar" Width="120" Style="{StaticResource PrimaryButton}" Click="OnConfirmClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
